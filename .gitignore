# dependencies
node_modules

# next.js
.next
next-env.d.ts
tsconfig.tsbuildinfo

# env
.env
.env.local
.env.*.local

# user uploads (KVKK / GDPR — never commit personal data)
public/uploads/faces
!public/uploads/faces/.gitkeep
!public/uploads/faces/test-face.png

# plant audio (committed to repo so production serves real ElevenLabs voices)
# regenerate locally with `npm run plants:audio`
public/audio/plants/_manifest.json

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor
.vscode
.idea
.DS_Store
Thumbs.db
.tmp_*

# Python bytecode
__pycache__/
*.py[cod]

# avatar pipeline scratch / working files (FBX, zip, previews — not for repo)
public/models/_wip/
public/models/_backup/

# shop video optimization backups (originals kept locally, optimized ones committed)
public/play/shop/_raw/

# product photo optimization backups
public/products/_raw/

# universe video optimization backups
public/universe/_raw/

# hero background video optimization backups
public/hero/_raw/

# Blender otomatik yedekleri
*.blend1
